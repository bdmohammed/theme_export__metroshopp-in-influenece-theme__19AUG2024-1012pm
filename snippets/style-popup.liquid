<template id="style-popup">
  <link
    href="{{ 'pre_flickity.css' | asset_url }}"
    rel="stylesheet"
    type="text/css"
    media="all"
  >
  <link
    href="{{ 'main-product.css' | asset_url }}"
    rel="stylesheet"
    type="text/css"
    media="all"
  >
  <link
    href="{{ 'qv-product.css' | asset_url }}"
    rel="stylesheet"
    type="text/css"
    media="all"
  >
  <link
    href="{{ 'drawer.css' | asset_url }}"
    rel="stylesheet"
    type="text/css"
    media="all"
  >
  <link
    href="{{ 'qs-product.css' | asset_url }}"
    rel="stylesheet"
    type="text/css"
    media="all"
  >
</template>
